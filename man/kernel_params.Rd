% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel_params.R
\name{kernel_params}
\alias{kernel_params}
\title{Kernel parameter recommendations}
\usage{
kernel_params(
  fraction = NULL,
  theta = NULL,
  d,
  loss = NULL,
  kernel = c("gaussian", "uniform", "inverse_distance"),
  data_dist = c("mvn", "uniform")
)
}
\arguments{
\item{fraction}{Target fraction of pool sites captured (in
weight-proportional terms) by the kernel for the typical focal.
Use this OR \code{theta}. Requires \code{data_dist = "mvn"}.}

\item{theta}{Bandwidth value. Use this OR \code{fraction}. For Gaussian
kernels, this is the standard bandwidth parameter; for uniform
kernels, this is the cutoff radius (also returned as \code{max}); for
inverse-distance kernels, this is the epsilon regularization that
determines the half-weight scale.}

\item{d}{Dimensionality of the space (e.g., number of climate
variables after Mahalanobis transformation, or 2 for geographic).}

\item{loss}{Fraction of aggregate kernel weight to discard at the
truncation distance \code{max}. If \code{NULL} (default), \code{max} is not
computed.}

\item{kernel}{One of \code{"gaussian"} (default), \code{"uniform"}, or
\code{"inverse_distance"}.}

\item{data_dist}{Distribution of cells in space. Either \code{"mvn"}
(multivariate standard normal; default; appropriate for
Mahalanobis-transformed climate data) or \code{"uniform"} (appropriate
for geographic space).}
}
\value{
A named list. For Gaussian and inverse-distance kernels:
element \code{theta}, and \code{max} if \code{loss} is specified. For uniform
kernels: element \code{max} (the single cutoff radius, which serves as
both bandwidth and truncation distance; supplied in the \code{analogs}
package as \code{max_clim} or \code{max_geog}).
}
\description{
Returns recommended values for the bandwidth (\code{theta}) and hard
truncation distance (\code{max}) of a kernel operating on Euclidean
distances in \code{d}-dimensional space. Gives theoretical answers to the
questions, "How big should \code{theta} be in order for my kernel to capture
a given fraction of pool sites for the typical focal site?" and "How
big should \code{max_clim} or \code{max_geog} be to truncate only a given
percentage of kernel weight for the typical focal site?" Accounts for
the effects of analog space multidimensionality on pairwise distance
distributions, which can result in one-dimensional intuitions being
incorrect. Supports the kernel types and data distributions used by
the \code{analogs} package.
}
\details{
Either \code{fraction} or \code{theta} should be provided. When \code{fraction} is
given, the function returns the \code{theta} that calibrates the kernel
to capture, on average, that fraction of pool sites (where partial
capture is in proportion to kernel weight). Switching kernel shapes
at fixed \code{fraction} holds the expected total kernel weight constant,
so weighted aggregate statistics (e.g. \code{sum_weights}) remain
comparable across kernels.

For uniform data, \code{fraction} is not meaningful because "fraction of
space" depends on landscape extent; \code{theta} must be supplied directly
(e.g. a dispersal-derived bandwidth for a geographic kernel).

When \code{loss} is specified, the function additionally returns \code{max}:
the truncation distance beyond which less than \code{loss} of aggregate
kernel weight is discarded. Useful for computational efficiency.

Recommendations are averages over the distribution of focal cells;
specific focal cells experience effective neighborhoods that vary
around these averages, with cells in dense climate regions seeing
more neighbors than cells in sparse regions.
}
\examples{
# Climate kernel: niche fraction of 5\% in 4 climate variables
kernel_params(fraction = 0.05, d = 4, loss = 0.01)

# Geographic kernel: 500 km dispersal-based bandwidth
kernel_params(theta = 500, d = 2, data_dist = "uniform", loss = 0.01)

# Switching kernels at fixed niche fraction (matched expected weight)
kernel_params(fraction = 0.05, d = 4, kernel = "gaussian")
kernel_params(fraction = 0.05, d = 4, kernel = "uniform")
kernel_params(fraction = 0.05, d = 4, kernel = "inverse_distance")
}
