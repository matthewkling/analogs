% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.predict_from_coefs}
\alias{.predict_from_coefs}
\title{Predict from regression coefficients}
\usage{
.predict_from_coefs(coefs_df, covariates_matrix, y_names, cov_names)
}
\arguments{
\item{coefs_df}{A data.frame with \code{coef_intercept} and \verb{coef_\{covname\}}
columns (single-y case), or \verb{coef_intercept_\{yname\}} and
\verb{coef_\{covname\}_\{yname\}} (multi-y case).}

\item{covariates_matrix}{Matrix with one row per focal and one column per
covariate. Column order must match \code{cov_names}.}

\item{y_names}{Character vector of y variable names.}

\item{cov_names}{Character vector of covariate names, matching the order
of columns in \code{covariates_matrix}.}
}
\value{
A numeric matrix with \code{nrow(covariates_matrix)} rows and
\code{length(y_names)} columns, named by \code{y_names}.
}
\description{
Shared helper that evaluates fitted values from an \code{analog_regression()} /
\code{analog_search(stat = "regression")} output by multiplying coefficient
columns with a matrix of covariate values (one row per focal).
}
\details{
Handles both single-y and multi-y coefficient layouts and returns a
matrix of predictions with n_focal rows and n_y columns.

This helper exists so that residual computation in \code{analog_cv()} and any
future user-facing prediction helper share the same arithmetic.
}
\keyword{internal}
