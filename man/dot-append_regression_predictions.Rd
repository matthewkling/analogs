% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analog_regression.R
\name{.append_regression_predictions}
\alias{.append_regression_predictions}
\title{Append \code{pred} column(s) to an analog_regression result}
\usage{
.append_regression_predictions(result, x_covariates, y, covariates)
}
\description{
Evaluates fitted values at focal locations by combining per-focal
regression coefficients in \code{result} with focal-side covariate values
in \code{x_covariates}. Handles both data.frame and SpatRaster \code{result},
both single-y and multi-y regression, and both data.frame/matrix and
SpatRaster \code{x_covariates} inputs. Uses \code{\link[=.predict_from_coefs]{.predict_from_coefs()}} for the
arithmetic.
}
\details{
Name resolution uses the package's existing validator helpers to pull
\code{y_names} / \code{cov_names} from the original \code{y} / \code{covariates} arguments,
avoiding any need to parse coefficient column names.
}
\keyword{internal}
