% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{metadata}
\alias{metadata}
\title{Get metadata from analog search results}
\usage{
metadata(x)
}
\arguments{
\item{x}{A result from any \verb{analog_*()} function (data.frame or
SpatRaster).}
}
\value{
A named list. Elements depend on which attributes are present on
\code{x}; missing attributes are simply absent from the returned list (no
\code{NA} placeholders). Possible elements include:
\subsection{Selection and aggregation parameters}{

\describe{
\item{\code{select}}{Selection strategy: \code{"all"}, \code{"knn_env"}, or
\code{"knn_geog"}.}
\item{\code{k}}{Number of neighbors for kNN selection modes; \code{NULL} for
\code{select = "all"}.}
\item{\code{stat}}{Aggregation statistic(s) requested. Character vector;
\code{"none"} for pair-mode results.}
\item{\code{kernel_env}}{Environmental weighting kernel shape: \code{"uniform"},
\code{"gaussian"}, or \code{"inverse"}. \code{"uniform"} when environment is not
distance-weighted.}
\item{\code{kernel_geog}}{Geographic weighting kernel shape: \code{"uniform"},
\code{"gaussian"}, or \code{"inverse"}. \code{"uniform"} when geography is not
distance-weighted.}
\item{\code{theta_env}}{Scale parameter for the environmental kernel (Gaussian
bandwidth or inverse half-weight distance); \code{NULL} if the environmental
kernel is uniform.}
\item{\code{theta_geog}}{Scale parameter for the geographic kernel; \code{NULL}
if the geographic kernel is uniform.}
\item{\code{lambda}}{Ridge penalty for \code{stat = "regression"}; \code{0} for
ordinary weighted least squares.}
\item{\code{se}}{Standard-error framing applied to SE-supporting stats:
\code{"none"}, \code{"ess"}, or \code{"design"}.}
\item{\code{max_env}}{Environmental-distance threshold used for analog
selection. Scalar for Euclidean / Mahalanobis radius, or
length-\code{n_env} for per-variable thresholds.}
\item{\code{max_geog}}{Geographic-distance threshold used for analog
selection. Units are km when \code{coord_type = "lonlat"}, projection
units otherwise.}
\item{\code{min_geog}}{Geographic-distance lower threshold (annulus inner
radius): analogs closer than this to the focal were excluded.
\code{NULL} when no lower bound was set. Same units as \code{max_geog}. Mainly
used as a spatial buffer for cross-validation.}
\item{\code{exclude_self}}{Logical: was each focal's own pool row
excluded from its analog neighborhood? \code{TRUE} for results from
\code{\link[=analog_cv]{analog_cv()}} (LOO) or from \code{analog_search(exclude_self = TRUE)}.}
}
}

\subsection{Input-data properties}{

\describe{
\item{\code{n_x}}{Number of focal locations in the input \code{x}.}
\item{\code{n_pool}}{Number of reference locations in the input \code{pool}.}
\item{\code{n_env}}{Number of environmental variables.}
\item{\code{coord_type}}{Coordinate system: \code{"lonlat"} (great-circle
distances in km) or \code{"projected"} (planar distances in projection
units).}
\item{\code{x_cov_provided}}{Logical: was a per-focal covariance matrix
(\code{x_cov}) supplied? If \code{TRUE}, environmental distances were computed in
Mahalanobis units using location-specific covariance; otherwise
Euclidean.}
\item{\code{downsample_actual}}{Actual downsampling rate applied to the
reference pool (1.0 if no downsampling). May exceed the requested
rate when maintaining at least one point per occupied bin
requires it.}
}
}

\subsection{Index diagnostics (lattice-related)}{

These attributes describe the internal lattice index used to
accelerate the search; they're useful for performance debugging but
not needed to reproduce the result. See \code{\link[=build_analog_index]{build_analog_index()}} for
detail.

\describe{
\item{\code{binning_method}}{Internal indexing method; one of
\code{"lattice"} or \code{"lattice_ecef"}.}
\item{\code{total_bins}, \code{n_bins_nonempty}, \code{min_bin_occupancy},
\code{max_bin_occupancy}, \code{avg_bin_occupancy},
\code{avg_nonempty_bin_occupancy}}{Lattice occupancy summaries.}
\item{\code{env_res_adj}, \code{geog_res_adj}}{Per-family resolution
adjustments used to build the index: each scales its family's bin
count relative to a data-dependent default (\code{1} = default, \code{0} =
deactivated). See \code{\link[=build_analog_index]{build_analog_index()}}.}
\item{\code{env_target}, \code{geo_target}}{Realized per-family bin-count
targets passed to the lattice builder (the absolute values the
\verb{*_res_adj} resolve to).}
\item{\code{bins_per_axis}}{Integer vector of realized bins per axis
(geographic axes first, then environment), showing how the budget was
distributed.}
}
}

\subsection{Cross-validation metadata}{

Present only when \code{x} is a result from \code{\link[=analog_cv]{analog_cv()}}.

\describe{
\item{\code{cv_method}}{\code{"loo"} or \code{"kfold"}.}
\item{\code{cv_fun}}{Name of the analog function being cross-validated
(e.g. \code{"analog_impact"}).}
\item{\code{cv_n_folds}}{Number of folds (k-fold only).}
\item{\code{cv_pred_target}}{Mechanism used to extract predictions for
residuals: \code{"weighted_mean"}, \code{"regression"}, or \code{"none"}.}
}
}
}
\description{
Returns the metadata attributes attached to results from \code{analog_search()},
or any \verb{analog_*()} wrapper function that calls it, as a named list. These
same values can be accessed individually via \code{attr(x, "<name>")};
\code{metadata()} is a convenience that bundles them into a consistent
list structure independent of the underlying attribute layout.
Use it to inspect or programmatically reconstruct the parameterization
that produced a result. Each attribute records one user-facing parameter
or input-data property.
}
\details{
\code{metadata()} is the documented entry point for reading these
attributes; the attribute names themselves are also part of the public
contract, so \code{attr(x, "select")} etc. are equally valid for individual
values. The function's main advantages over raw \code{attributes()} are: it
filters out R-internal attributes (\code{names}, \code{class}, \code{dim}, ...), and
the \code{?metadata} help page gives a single canonical reference for
every attribute the package attaches.
}
\examples{
\dontrun{
result <- analog_velocity(
  x = future_climate,
  pool = current_climate,
  env = kernel(max = 0.5)
)

# Get the full parameterization as a list
meta <- metadata(result)
meta$select
meta$max_env
meta$n_pool

# Or read individual attributes directly
attr(result, "select")
}

}
