# Validate and normalize query parameters

Validates mode/k/weight/theta combinations and normalizes values.
Returns a list with normalized parameters.

## Usage

``` r
.validate_query_params(mode, k, weight, theta)
```
