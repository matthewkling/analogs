# Search lattice index for analogs

Search lattice index for analogs

## Usage

``` r
query_analog_index(
  x,
  index,
  select,
  stat,
  max_env,
  max_geog,
  x_cov,
  y,
  covariates,
  weight = NULL,
  k,
  kernel_env,
  kernel_geog,
  theta_env,
  theta_geog,
  lambda,
  se,
  normalize = "auto",
  exclude_self = FALSE,
  n_threads,
  show_progress = FALSE
)
```
