# Search lattice index for analogs

Search lattice index for analogs

## Usage

``` r
query_analog_index(
  x,
  index,
  select,
  stat,
  max_clim,
  max_geog,
  x_cov,
  y,
  covariates,
  k,
  kernel,
  theta,
  lambda,
  se,
  exclude_self = FALSE,
  n_threads,
  show_progress = FALSE
)
```
