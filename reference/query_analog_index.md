# Search lattice index for analogs

Search lattice index for analogs

## Usage

``` r
query_analog_index(
  x,
  index,
  mode,
  max_clim,
  max_geog,
  k,
  weight,
  theta,
  report_dist,
  n_threads
)
```
