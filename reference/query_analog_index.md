# Search lattice index for analogs

Search lattice index for analogs

## Usage

``` r
query_analog_index(
  x,
  index,
  select,
  stat,
  max_clim,
  max_geog,
  x_cov,
  k,
  weight,
  theta,
  report_dist,
  n_threads
)
```
