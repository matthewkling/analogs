# Search lattice index for analogs

Search lattice index for analogs

## Usage

``` r
query_analog_index(
  x,
  index,
  select,
  stat,
  max_clim,
  max_geog,
  x_cov,
  values,
  k,
  weight,
  theta,
  n_threads
)
```
